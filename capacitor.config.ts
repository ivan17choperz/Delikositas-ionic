import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.delikosita.app',
  appName: 'delikositas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
